import * as THREE from 'three';

export function CreateLever(scene, position, rotation, callback) {
   const scale = 1;

   function createLever() {
      //Load lever 3d model
   }

   createLever();
}